[build]
  base = "frontend"
  command = "npm install && npm run build"
  publish = ".next"

[build.environment]
  SECRETS_SCAN_OMIT_KEYS = "NEXT_PUBLIC_API_URL,BACKEND_URL"

[[plugins]]
  package = "@netlify/plugin-nextjs"
